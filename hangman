from random import choice, randint

words = [horse, carrot, dog, cat, mole, donkey, dragon]
words_count = int(len.words)
choosen_wordindex = random.randint (0, (int(wods_count) - 1))
chossen_word = words[int(choosen_wordindex)]
life = [10]
prev_bets = []


def try_letter (bet):
	if choosen_wold.count('bet') > 0:
	...
	else:
	print("Wrong bet!)
	life[0] = life[0] - 1
	print_state ()







def print_state:

	print (life + "LIFE LEFT!")


			
			
		else:
			print ("Your bet is invalid! Try again.)
			continue

	
